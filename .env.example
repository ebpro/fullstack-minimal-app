# Environment Configuration Template
# Copy this file to .env and customize for your environment
# NEVER commit .env to git - it contains sensitive information!

# ============================================================================
# MySQL Database Configuration (Student Notes)
# ============================================================================
# These credentials are used by the MySQL container and the backend service
# For production: Use strong, unique passwords and consider secrets management

MYSQL_ROOT_PASSWORD=rootpassword
MYSQL_DATABASE=minimal_app_db
MYSQL_USER=appuser
MYSQL_PASSWORD=apppassword

# ============================================================================
# Backend Service Configuration
# ============================================================================
# Database connection settings - should match MySQL config above
DB_HOST=mysql
DB_PORT=3306
DB_USER=appuser
DB_PASSWORD=apppassword
DB_NAME=minimal_app_db

# Backend server settings
PORT=4000
NODE_ENV=development

# CORS settings - frontend URL that's allowed to make requests
FRONTEND_ORIGIN=http://localhost:5173

# ============================================================================
# Frontend Configuration
# ============================================================================
# Backend API endpoint for the frontend to connect to
VITE_BACKEND_URL=http://localhost:4000

# ============================================================================
# Development vs Production Notes for Students
# ============================================================================
# DEVELOPMENT (current setup):
# - Uses simple passwords for convenience
# - MySQL port exposed for database tools (Adminer, MySQL Workbench)
# - Detailed error messages enabled
# - CORS allows localhost requests
#
# PRODUCTION changes needed:
# - Generate strong, unique passwords
# - Remove MySQL port exposure (comment out ports section)
# - Set NODE_ENV=production
# - Use specific domains for FRONTEND_ORIGIN (not localhost)
# - Enable SSL/TLS for database connections
# - Use secrets management (Docker secrets, AWS Secrets Manager, etc.)
